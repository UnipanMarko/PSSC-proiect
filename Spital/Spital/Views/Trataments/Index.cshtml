@model IEnumerable<Spital.Models.Tratament>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nume)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdTratament }) |
                @Html.ActionLink("Details", "Details", new { id=item.IdTratament }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdTratament })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
